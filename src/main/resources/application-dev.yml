spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  devtools:
    restart:
      enabled: false

lopet:
  bootstrap:
    seed-demo: ${SEED_DEMO:true}